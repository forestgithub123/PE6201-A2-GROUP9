
---

# 更新（2026-09-16）：D2 / D3 已完成，交接说明

以下这部分是这次会话做完 D2、D3 之后写的交接更新，放在最上面方便直接看。
原始任务说明（我出发点用的那份）保留在下面，供对照参考。

## 一、这次做完了什么

**D2（工具层）和 D3（安全护栏）两部分基本完成**，正式交付物是：

- [`docs/D2_tool_layer.md`](A2_scaffold/docs/D2_tool_layer.md) —— D2(a)(b)(c)
  完整书面交付，工具集去留分析、依赖/并行设计、v1→v2 prompt 对比（真实
  live 数据），**这是最主要的参考文档，接手先看这个**。
- [`docs/D3_guardrails.md`](A2_scaffold/docs/D3_guardrails.md) —— D3(a)(b)
  完整书面交付，护栏上限数值依据、11 个测试用例（含 3 个恶意 narrative）。

代码层面动了这些文件（都在 `A2_scaffold/` 下）：

| 文件 | 改了什么 |
|---|---|
| `config.py` | `BACKEND` 默认值改回 `"scripted"`（干净环境不设 key 也能跑，这是提交红线要求），保留 `A2_BACKEND` 环境变量覆盖 |
| `backends.py` | 修了 live backend token 统计永远是 0 的 bug（现在读取 OpenRouter 真实 usage）；修了模型返回畸形 JSON 时会直接 crash 的 bug |
| `tools.py` | `check_duplicate_claim` 签名从 4 个参数改成 `claim_id` 一个参数（工具内部自己查 claim，防止模型抄错 `lines`）；7 个 Problem A 工具的 descriptor 补了 `irreversible` 字段；`issue_decision_letter` 现在真的往 `decisions.jsonl` 追加记录，不再只返回一个 dict |
| `prompt.py` | 重写了 Problem A 的 routing rules 和回答格式说明（补充 injection 防御、escalate/request_document 判断顺序、annual_limit 算术步骤）；新增 `RULES_V1`/`_HOW_TO_ANSWER_V1` 保留原文，做成 `version="v1"/"v2"` 开关，供以后随时复现对比 |
| `agent.py` | 新增运行时校验：escalate 必须带 trigger 且要有工具证据支撑、request_document 必须带 missing；新增对"模型把决策标签当工具名调用"的纠正处理；新增 `STRICT_VALIDATION` 开关（配合 prompt 的 v1/v2 开关）；`MAX_FINAL_REJECTIONS` 从 4 调到 6（有实测依据，见 D2 文档） |
| `.gitignore` | 加了 `decisions.jsonl` |

新增的开发/测试脚本（都在 `A2_scaffold/` 下，不影响 `run_eval.py` 评分流程）：

- `guardrail_tests.py` —— D3(b) 的 11 个用例，跑 `python3 guardrail_tests.py`，全 scripted 免费，目前 11/11 PASS
- `dev_seq_vs_parallel.py` —— D2(c) 串行 vs 并行对比，scripted，免费（**注意里面的 token/cost 是模拟估算，不是真实模型成本，不要直接喂给 D6**）
- `dev_v1_v2_compare.py` —— D2(b) 的 v1/v2 live 对比，**会花钱**（一次完整跑约 $0.07，用的是 `openai/gpt-4o-mini`），需要 `OPENROUTER_API_KEY`

以及三个数据文件（跑出来的原始结果，供报告引用）：`guardrail_test_results.json`、`dev_seq_vs_parallel_results.json`、`dev_v1_v2_compare_results.json`。

## 二、几个值得知道的关键发现

- **v1（原始 prompt）在真实 live 模型上通过率只有 18.2%（6/33），v2（改写后）到 72.7%（24/33）**——同一份代码、同一个模型、同样的试验次数测出来的，不是两次不对等的实验。
- 残余的失败案例（annual_limit 算术错误、2 个 prompt-injection 案例）额外用 `gpt-4o` 测过，**全部正确**——证明是 `gpt-4o-mini` 这个价格档位的能力上限，不是 prompt/工具设计问题。这个结论对 D6 的模型分级分析很有用。
- 之前 live backend 的 token 统计一直是 0（scaffold 遗留的已知缺口），意味着 `budget_ceiling` 这个护栏在 live 场景下**从来没有真正生效过**——已经修好。
- `check_duplicate_claim` 的签名改了（`claim_id` 单参数），如果有同学的脚本/notebook 里手写过老的四参数调用方式，需要改一下。

## 三、后面的同学从哪里接手

**如果是继续打磨 D2/D3**：
1. 先读 `docs/D2_tool_layer.md` 和 `docs/D3_guardrails.md`，里面每个结论都标了怎么复现。
2. D2(a) 的工具集决定（尤其是 `check_duplicate_claim` 改签名、`check_coverage` 不拆分）建议在组内过一遍——这些是有数据支撑的判断，但毕竟改了共用接口，最好组内确认一下没人反对。
3. 如果要在真实 live battery（D5b）里验证更多模型，直接跑 `dev_v1_v2_compare.py <model_name>` 即可，脚本会自动按 D4 惯例给负面案例跑 3 次。

**如果是做 D4/D5/D6/D7 的同学，这几点会影响你们**：
- `decisions.jsonl` 现在会真的落盘了，D4 扩充案例后跑测试时会看到这个文件持续增长，属于正常现象（累积了所有跑过 `issue_decision_letter` 的记录，包括测试和 demo）。
- D5(b) 的模型 battery 现在可以拿到真实 token/cost 数字了（`backends.py` 的修复），D6 直接用这些数字，不用再自己估算。
- D6 千万不要把 `dev_seq_vs_parallel.py` 里的 token/cost 数字当真实成本用，那是 scripted 模拟值，只能证明"串行更贵"这个机制，不能算钱。
- `config.py` 的 `BACKEND` 默认值现在是 `scripted`——D5(a) 的"干净环境跑通"要求已经满足，不用再改。

## 四、还没做 / 需要确认的事

- D2(a) 里几个工具去留的判断没有正式经过组内讨论确认（见上）。
- `dev_seq_vs_parallel.py`/`dev_v1_v2_compare.py` 这几个脚本是不是要留在最终提交的仓库里，还是整理进正式的 D5(b) battery 脚本，需要团队决定。
- 目前改动都在工作区，**还没有 `git commit`**，需要谁来做最终的提交。
- 这次 live 测试总共花了大约 $0.2（都是 `gpt-4o-mini`/`gpt-4o` 的小额调用），供报销/记账参考。

---

## D2 工具层交接说明（Problem A）

目前 Problem A 脚手架已经可以通过以下命令完成 `CLM-8842` 的单案例测试：

```bash
cd A2_scaffold
python3 run_eval.py CLM-8842
```

接下来请负责 D2 的同学评估当前工具集是否健全，是否需要精简、合并或优化。不要直接假设老师提供的工具都必须保留，也不要急着增加新工具。

### 当前工具集

请检查 [`tools.py`](/Users/forstmac/Desktop/NTU/PE6201/A2/A2_scaffold/tools.py) 中 Problem A 的工具：

```text
get_claim
lookup_policy
lookup_hospital
check_coverage
get_preauthorisation
check_duplicate_claim
issue_decision_letter
```

目前 `check_coverage` 已经扩展了 required-document 检查，并支持：

```text
member_id
procedure code
attached documents
```

它会返回：

```text
coverage 状态
exclusion
是否需要 pre-authorisation
required documents
missing documents
```

### D2(a)：评估工具集

请对每个工具回答：

1. 删除它后，哪个具体案例会失败？
2. 是否容易和其他工具混淆？
3. 是否与其他工具功能重叠？
4. 能否合并到现有工具，减少工具数量？
5. 参数是否容易导致模型传错 ID 或漏传信息？
6. 返回结果是否足以支持最终决策？
7. 它是只读还是不可逆操作？
8. 即使从未调用，其 descriptor 是否仍增加每轮 token 成本？

不能只用“老师提供了它”作为保留理由。

### 重点问题

#### `check_coverage` 是否承担过多职责

它现在同时处理：

```text
procedure information
policy exclusion
pre-authorisation requirement
required-document requirement
missing-document comparison
```

需要分析：

- 合并后是否减少 turns 和漏查；
- 返回值是否过长或职责过多；
- 是否应拆出 `check_required_documents`；
- 拆分后会不会增加遗漏文件检查的风险。

#### `lookup_policy` 与 `check_coverage` 是否重复

两者都会接触 policy 数据：

```text
lookup_policy：
  status、有效日期、annual limit、remaining

check_coverage：
  根据 member_id 找到 policy，再检查单条 procedure
```

需要判断这种有限重复是否合理。当前设计的理由是让它们在取得 claim 后能够并行，但要测量是否真的降低 turns 和成本。

#### `lookup_hospital` 是否值得独立存在

非 panel hospital 不会直接改变 decision，但必须写入最终记录。需要说明：

- 为什么它不能删除；
- 是否应合并进 `get_claim`；
- 独立查询带来的证据可追踪性是否值得额外工具成本。

#### `check_duplicate_claim` 是否应保留

它需要比较：

```text
member
hospital
date of service
全部 lines
```

重点检查接口是否能防止模型只比较部分字段。可以考虑让它接收 `claim_id`，由工具内部读取完整 claim，避免模型重新传递整个 `lines` 数组。

#### `get_preauthorisation` 的调用条件

只能在：

```text
check_coverage.requires_preauth == true
```

时调用。

需要保证：

- member、procedure 和 service date 全部匹配；
- 找不到有效授权时返回明确语义；
- `None` 表示 request document，不是拒付；
- 不为无需授权的 line 进行多余查询。

#### `issue_decision_letter`

这是唯一 gated action，代表正式记录理赔决定。

需要保证：

- 所有必要证据完成后才允许调用；
- totals 和 line 数量在 gate 前校验；
- 每个 case 最多调用一次；
- `AUTONOMY="confirm"` 时需要人工确认；
- 只模拟本地记录，不进行真实邮件或支付操作。

### D2(b)：Descriptor

运行：

```bash
python3 run_eval.py --prompt
```

查看 live model 实际收到的工具说明。

每个工具的 descriptor 应包含：

```text
NAME + SIGNATURE
WHAT
WHEN
INPUT
RETURNS + SIZE BOUND
FAILS WHEN
IRREVERSIBLE? + GATE
```

请选择至少一个工具制作 v1/v2，并在同一模型、同一数据和同一配置下比较：

```text
pass rate
turns
input/output tokens
cost
guardrail cases passed
```

### D2(c)：依赖与并行

建议基准依赖关系：

```text
Turn 1:
  get_claim
```

取得 claim 后，以下调用只依赖 claim，可以放在同一轮：

```text
lookup_policy
lookup_hospital
check_duplicate_claim
check_coverage × 每条 line
```

之后：

```text
若 requires_preauth=true：
  get_preauthorisation
```

所有事实确认后：

```text
issue_decision_letter
```

最后才输出 final record。

请比较 sequential 和 parallel 两种执行方式的：

```text
turns
tokens
cost
pass rate
```

### 预期交付

请提供：

- 最终 Problem A 工具清单；
- 每个工具保留、删除、合并或拆分的理由；
- 支撑结论的具体 claim；
- 工具依赖和并行关系；
- 完整 descriptors；
- 至少两个 poka-yoke 设计；
- v1/v2 对比方案；
- sequential/parallel 测试方案；
- `CLM-8842` 回归结果。
